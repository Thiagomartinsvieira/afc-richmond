# Use root/example as user/password credentials
version: '3.1'

services:

  mongo:
    image: mongo
    restart: always
    container_name: afc-richmond

  mongo-express:
    image: mongo-express
    restart: always
    ports:
      - 8081:8081
